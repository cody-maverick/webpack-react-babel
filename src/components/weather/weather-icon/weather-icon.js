import React from 'react';

import './weather-icon.less'

const WeatherIcon = () => {
    let classNames = 'wi wi-icon-main wi-day-cloudy';
  return (
      <i className={classNames}>

      </i>
  )
};

export default WeatherIcon;